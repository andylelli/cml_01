{
  "name": "@cml/rubric-score",
  "version": "0.1.0",
  "private": true,
  "description": "Final-story rubric scorer: LLM-critic-primary, checker-gated holistic /100 mystery score (documentation/14_chat-gpt-scoring-alignment/aligning-the-scoring-system.md). Deterministic cap engine + facts + prompt; the LLM judge is an injectable seam. Pure, NOT wired into the pipeline.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "@cml/prose-guard": "file:../prose-guard" },
  "devDependencies": { "@types/node": "^20.11.0", "typescript": "^5.5.4", "vitest": "^1.2.0" }
}
