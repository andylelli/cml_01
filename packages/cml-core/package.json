{
  "name": "@cml/cml-core",
  "version": "0.1.0",
  "private": true,
  "description": "Typed logical core for CML + deterministic fair-play engines (timeline / deducibility / uniqueness). Keystone (P1) of the Agent 3 redesign (documentation/12_system_redesign/07_agent_3_cml_generator.md). Pure, no external deps, NOT yet wired into the pipeline.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^1.2.0"
  }
}
