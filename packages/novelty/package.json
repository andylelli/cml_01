{
  "name": "@cml/novelty",
  "version": "0.1.0",
  "private": true,
  "description": "Novelty as abstract structural fingerprints + an explainable verdict (Agent 8 redesign, documentation/12_system_redesign/14_agent_8_novelty_auditor.md). Seed + genre-cliche ledgers, deterministic structural compare. Pure, NOT wired into the pipeline.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "ajv": "^8.17.1", "js-yaml": "^4.1.0" },
  "devDependencies": { "@types/js-yaml": "^4.0.9", "@types/node": "^20.11.0", "typescript": "^5.5.4", "vitest": "^1.2.0" }
}
