{
  "name": "cml",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @cml/api build",
    "dev": "npm run -w @cml/api build && npm run -w @cml/api dev",
    "lint": "echo \"lint: TODO\"",
    "test": "npm run -w @cml/api test ; npm run -w @cml/worker test ; npm run -w @cml/web test"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
