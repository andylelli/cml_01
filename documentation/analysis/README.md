# Analysis Index

This folder tracks run-level failure analysis reports and consolidated issue lists.

## Latest
- `ANALYSIS_80/ANALYSIS_80.md` — **a three-word regex broke the mystery (2026-08-31).** Deep dive on run `mystery-1788202899854` and the external read that scored it 74/100. **THE SHAPE OF THE SCORECARD IS THE FINDING:** eight of eleven categories sit at 7–8 and two are outliers — clue logic **4** and ending **5** — and both are the SAME defect, so this is one arithmetic failure costing ~6 marks, not ten quality problems. Chapter 8's first draft stated the case's arithmetic correctly (*"set the hands from half past ten to twenty-five minutes past ten"*); a control-plane-leakage rule matched the ordinary English phrase **`required to`**, flagged it `major`, forced a regeneration, and the shipped chapter says *"from twenty-five minutes past ten to twenty-five minutes past ten"* — the exact line the reviewer calls nonsensical. **MEASURED over two known-clean populations (`scripts/leakage-falsepositive.mjs`): `instruction_shape` fires on 2 of 12 REAL GOLDEN AGE NOVELS (16.7%)** — a rule that fires on Wilkie Collins cannot tell prompt leakage from English — while **17 of the 19 rules are clean against canon**, so the defect is narrow and the family is sound. Separately `retry_term` fires at **critical** on **20% of our own manuscripts** (a B1 off-switch-with-extra-steps lead, unexamined). §3: the locked-fact guard checks PRESENCE not correctness — the manuscript says one time 40 times and the other twice, and a 40:2 collapse passes a presence check by definition, even though the prose prompt stated both correctly (so not a prompt bug). §4: the injector's cap counts pre-existing matches, so it appended a sentence identical to one already there — the 2 duplicate sentences the reviewer quotes. §5: the reveal was bound to the wrong chapter AND **the detector's own two messages disagree about which chapter discloses** (10 vs 6) — a locator that cannot locate. §6: the device layer and the CML layer hold different times (`fifty-five to ten` vs the two real ones) and the check only WARNS; reach 4/0/10/4/0/0 across six runs, so pre-existing and intermittent. §8: the run was excluded from its own novelty ledger because two records carrying `"severity":"warning"` were written into an `errors` array — biasing the corpus the pipeline diverges from toward clean runs only. **§9 ATTRIBUTION: none of it is the A_79 corpus work** (A1 never reached a prompt, C was shadow, D is read-only, and B's output — the case — was correct); one open question about exemplar-driven monoculture is stated as INFERRED with its falsification. **§12 is the real finding: SIX OF THE EIGHT DEFECTS WERE ALREADY IN THE RUN'S OWN TELEMETRY.** The external reader found the consequences of defects the system had named itself, every warning was advisory, and `hard_stop_count` was 0 — the pipeline's diagnosis is better than its shipping decision. §10 ranks 11 fixes; F1+F2+F3 are small, independent, and close the expensive one.
- `ANALYSIS_79/ANALYSIS_79.md` — **the integration plan: 12 encoded works, 0 reaching a prompt (2026-08-31).** Companion to A_77 (the corpus) and A_78 (how it is leveraged). Twelve public-domain works are now encoded, verified and validator-clean — case, fingerprint and device pattern each — and **a repo-wide grep for `library/works` outside the ingestion scripts returns zero hits.** The same defect A_78 diagnosed in the old corpus, rebuilt one directory over. MEASURED: installing just the device patterns takes Agent 3b's raw material from **11 patterns to 23** and nearly doubles it on three axes (spatial 5→10, behavioral 4→8, identity 1→3; `authority` stays flat, which is the honest reading — that gap is acquisition, not wiring). **Six connection points, four of them positive or obligation-shaped** rather than the prohibitions A_78 §7 found were the only existing links. **Phases A and B are FREE and hold nearly all the value**: A1 install the device patterns (prefer an extra-dir env var over copying — duplication is how `examples/` and `seed-fingerprints.yaml` drifted apart); A2 make the fingerprint ledger genuinely *derived* so the 11-of-14 drift becomes structurally impossible, and retire `the_second_key`, a synthetic CC-BY placeholder currently the **3rd most-cited nearest neighbour across 32 archived audits**; B repair Agent 3's three dead exemplar fields **before** repointing them, or our books print `Mechanism type: unknown` too. C gives `cell-scheduler` the corpus parameter it has never had, which finally computes the most valuable list available — **cells the canon occupies and we never have**. D is the anti-copy gate, the only item that is a *guarantee*: the wiki's input side is already safe (role-aware de-specification, verified clean, no proper nouns outside `sources:`), but nothing checks a finished chapter against the source texts, and that must ship BEFORE E puts real period prose near a prompt. E is the calibration corpus — 685k words of real Golden Age prose on disk, four deterministic measures, the first upward-pointing prose signal against A_75. §7 lists the blockers: the **three mechanism vocabularies (14/4/12)** and an enum that cannot express *The Invisible Man* or *A Jury of Her Peers* (add `role_invisibility`, `investigative_blind_spot`); `isDetectiveArchetype` matching "detective" anywhere in a comma-separated head; and *The Moonstone* at 259,232 tokens against a 250,000 ceiling. **Total: ~£0.30 and ~4 days — the integration is essentially free, because today's £1.71 bought the corpus and connecting it is engineering, not tokens.** §9 states the falsifications first (A1 changing nothing measurable is the cheapest test); §10 the do-nots. **§11 IS THE IMPLEMENTATION RECORD (2026-08-31): A, B and C are BUILT, flag-gated default-OFF, verified against the built code — 2,244 tests green across five packages.** A1 `DEVICE_LIBRARY_INCLUDE_CORPUS` (11→23 patterns); A2 the ledger is genuinely derived and `--check`-guarded; B `SEED_CORPUS_FROM_LIBRARY` turns *Mechanism type: unknown / 0 constraints / 0 steps* into a worked case (premise, false belief, mechanism, inference chain, discriminating test) with character names replaced by their own roles; C `NOVELTY_CELL_SCHEDULER_CORPUS`. **MEASURED for C: of 70 cells, 5 are occupied by both, 5 by CANON ONLY, 12 by us only and 48 by neither — and 44 of our 102 runs sit in `temporal × locked_room_timing` alone.** The corpus-blind pick was `behavioral × alibi_fabrication`, drawn from the 48; with the corpus it becomes `behavioral × impersonation`, evidenced by *The Invisible Man*. Canon-occupancy breaks ties BELOW depth, and an empty corpus provably leaves the pick unchanged. A separate B repair found **five legacy seeds were being handed to Agent 3 as filenames** — literally *diverge from trents_last_case_cml2.yaml* — and that the alphabetical `slice(0, 8)` meant, once the downstream merge reserves only three seed slots, that *Trent's Last Case* and *The Mysterious Affair at Styles* could **never** be named. **§11.3 WITHDRAWS §7.4's "the cases are structurally sound".** Wiring A2 broke a load-bearing novelty invariant: a paraphrase of *The Big Bow Mystery* stopped reading as a clone. **And the encode had not INVENTED anything — it had lifted the book's own REFUTED theory** (the murderer entering beforehand with a key) and presented it as the truth; Zangwill's actual trick is that the killer cuts the throat AS HE BREAKS THE DOOR DOWN, confirmed against the text. **A detective novel contains a fully-argued wrong answer by design**, so an encoder that grabs it produces a case that is coherent, textually grounded, schema-valid and exactly backwards — a worse failure than invention and far easier to miss. **The evidence was already on disk and nothing read it:** `encode-report.json` recorded that mechanism anchor as `ok: false`, but promotion gated on `validateCml`, a SCHEMA check, which a coherent wrong answer passes as easily as the truth. Promotion now gates on anchor evidence in three states (*we looked and it failed* ≠ *we never looked*). **Four works were re-encoded against a corrected prompt: Big Bow 9/20 → 15/20, its delivery_path now breaking the door BEFORE the throat is cut, and the classifier then independently re-derived the hand-authored fingerprint that had been right all along.** One demotion was WITHDRAWN — `the_valley_of_fear` was cut on a stale 8/20 written before its case was last repaired; recomputing gives 15/20. Final: **4 `derived`, 5 `derived_unverified`, 8 legacy** (Hound, Yellow Room and Leavenworth still demoted). A competing explanation was tested and REFUTED: the matcher collapses whitespace in the anchor but not the source, so a hard-wrapped anchor could never match — real bug, but these texts are stored de-wrapped and it recovers 0 anchors; fixed anyway. **The gate now reaches A1 and B** via `library/works/.verification.json` (A1 23→20 patterns, B 9 of 12 cases); only `failed` is excluded, and a missing manifest skips nothing. §11.4: **five tooling defects**, of which two matter most — a rejected re-encode used to overwrite the accepted case's evidence, so the gate judged one file by another file's anchors (caught live on Leavenworth by a fail-closed guard), and `false_assumption_pattern` was literally **the first four words of a sentence** (`the_bloodstain_on_the`), a similarity key the novelty judge depends on, making the derived fingerprints WEAKER than the hand-authored ones they replaced. §11.5: D and E unstarted; Hound's anchors join words from different places, Leavenworth has consumed £0.63 across five attempts and needs a different approach, not another reroll. Spend this pass **£0.67**, £2.35 cumulative. **§12 IS PHASE D, THE ANTI-COPY GATE — the only item in A_79 that is a GUARANTEE rather than an improvement** (flag `PROSE_ANTI_COPY_GATE`, default OFF): a hash-set lookup over normalised word n-grams that hard-fails any manuscript reproducing a verbatim run from `library/texts/` (719,552 words, 12 works, now committed — they had existed only in a session scratch dir). **§5's own proposal was a 6-gram, and a 6-gram fires on 92.2% of OUR OWN manuscripts as a hard fail** — CLAUDE.md B1's off-switch-with-extra-steps, caught only because the baseline ran first over 204 known negatives (every hit a false positive BY CONSTRUCTION, since no source prose has ever reached a prompt): n=6 92.2%, n=7 28.9%, n=8 2.9%, **n=10 0.0%**. A synthetic 40-word lift from *The Moonstone* is caught at every n and survives re-casing and re-punctuation, so the quiet at n=10 is selectivity not blindness. Runs LAST in `agent9-run.ts`, after the final re-validation, because the post-processing chain writes prose after the release gate decides; `ctx.prose` is assigned BEFORE the throw so the manuscript survives for diagnosis, and a missing corpus fails OPEN. **§13 IS PHASE E, the calibration corpus** — four deterministic measures, canon vs our 204 manuscripts, zero LLM cost. **§13.1 CORRECTS §6:** it argued a canon-derived sentence-length target would be obeyed where voice-spec's invented 22.0 was not, but A_75's matched pair got 15.01 when it asked for 19.5 while semicolons went 5→13 when it asked for semicolon-linked clauses — provenance does not change compliance, so the measure separates DISTRIBUTIONS (diagnosis) from OPERATIONS (instruction). **§13.2 is the finding: the register gap is NOT sentence length.** Mean 16.09 vs 14.89 and median 13.11 vs 13.38 are already at canon — what VoiceSpec spent its life asking for was never the gap. The gap is the TAIL (p90 31.02 vs 25.63, sentences >30 words 10.4% vs 4.0%) and three countable operations, the largest being **paragraphs opening on speech: canon 59.7% vs ours 11.5%**, plus em-dashes 5.9× and semicolons 3.3×. §13.3: we FRONT-LOAD MORE orientation than the canon (names a person 98.5% vs 75%, states a place 63.2% vs 25%), and our mysteries happen in one room and end in a confession while the canon travels (0.34 vs **0.01**), corresponds and holds inquests; the anachronism list §6.3 wanted for `period-kb` is real once our own cast is excluded (`lobby, timeline, logs, radio, ledger, clocks, alibis, chime, ticking`). **§13.4: three probe bugs caught before persisting**, the worst reading the canon as 1.02% dialogue because literal curly quotes were flattened on write and Gutenberg texts contain ZERO straight double quotes — spotted only because it contradicted its own neighbour. §13.5: the measures are ARTIFACTS, not wiring — nothing reaches a prompt while D is off, and Phase D verifies E's own outputs carry no copied prose.
- `ANALYSIS_78/ANALYSIS_78.md` — **the corpus supplies the generator with prohibitions, and the idea engine reads something else entirely (2026-08-31).** Companion to A_77: that asked *is the corpus wired up*, this asks **what the canon contributes to the invention of a new mystery** — and the two come apart, because a prohibition is a connection but not an ingredient. MEASURED on the live 2026-08-31 prompts: **Agent 3b is the idea engine** (A_72 §2.1: the one component that earns the reader's 9s) and its 18,982-char prompt draws **81 characters — 0.43% — from any real novel, all of it three titles in a diverge-from list**, one of which is a raw filename. Its actual raw material is 2,768 chars from a **separate hand-authored corpus of 11 device patterns citing free-text "convention"**. **Not one character of worked material from a real novel reaches either idea prompt.** Agent 3's larger share (2,134 / 56,259 = 3.8%) is entirely prohibition or content-free — its one positive channel prints `Mechanism type: unknown / 0 constraints / 0 steps`. **§6 IS THE ACTIONABLE FINDING: the device library re-describes from memory what the corpus holds as data** — `recorded_presence` cites *"Sayers/Christie-era gramophone tricks"* while we own *The Clue of the Twisted Candle*, which IS that trick, encoded; same for `delayed_poison` vs *Styles*, `substituted_identity` vs *Valley of Fear*, `trusted_authority_lie` vs *Big Bow* + *Yellow Room*. The corpus was digested into a wiki and the wiki is what the generator reads — **which is why acquiring novels has never changed anything: the acquisition path and the generation path are not joined.** §4: `cell-scheduler.ts` — the one component whose own header argues prohibitions fail and obligations work — takes `scheduleCell(priorRuns)` and **has no parameter for the corpus**, so its map is of US not the genre (102 records, 21 of 504 cells, and **97 of 102 lack the `mechanismFamily` field** the mechanism turns on). §5: **three mechanism vocabularies** — 14 (corpus), 4 (device library, i.e. the engine), 12 (scheduler) — with `FAMILY_MAP` bridging 14→4 and **five mapping to `null`**, which suppresses the raw-material block entirely. Tenth instance of the one-vocabulary-many-spellings failure, first where it separates the corpus from the ENGINE. Coda: the only real novel title hard-coded in `prompts-llm` is *"The Moonstone"* — as a **placeholder in Agent 8's example JSON**. **§9 ANSWERS "can the encoded novels become the wiki?" — YES, prototyped and measured, not argued.** A derivation from `library/works/*/case.cml2.yaml` to `schema/device_library.schema.yaml` was built and run over all four encoded works (drafts at `library/works/*/device.draft.yaml`): **10 of 11 required fields derive**, all four validate against the real ajv schema, and **retrieval integration measurably improves coverage for a 1931 story — identity 1->2, behavioral 4->6, spatial 5->6**. `sources` stops being a gesture: instead of *"Impersonation / substitution convention"* a derived pattern cites *"Baroness Orczy, The Fenchurch Street Mystery — library/works/…/case.cml2.yaml"*, a path resolving to a validator-clean case. **Three traps appeared only on running it: (1)** `eras` derived from the publication decade produces four schema-valid patterns that retrieve **ZERO times** — `eras` is a feasibility RANGE, not a provenance date; **(2)** empty `requires`/`forbids` is not neutral, it means **feasible in every era including 2020** — a derived pattern silently disables its own anachronism floor; **(3)** an unknown capability token **fails safe in `requires` and fails OPEN in `forbids`** (demonstrated: a pattern given a non-existent `forensic_scene_protocol` token reports feasible in 2020 while its three siblings correctly do not). Plus the A_77 §16.5 mis-tags **propagate** — 2 of 4 needed a `family` override, because a bad `mechanism_family` becomes a wrong retrieval key. **§9.4: de-specification is real work and a regex is the wrong tool** — the first pass leaked `Kershaw`/`Brodski`/`Wright` into supposedly abstract templates, and the *fixed* version collapses three different people into "the actor", destroying the distinction that IS the mystery; it needs role-aware rewriting from `cast[].role`, one LLM call. (The failed fix is itself recorded: written as a heredoc, `\b` collapsed to a backspace escape, so the negative result was a claim about the probe — the exact CLAUDE.md trap.) **§9.5 is the one genuinely EXTERNAL gap, now researched: the 19-token era-capability vocabulary needs 3 new members** — `forensic_scene_protocol` (Gross 1893 / English 1906, Locard's lab 1910), `fingerprint_classification` (Galton-Henry, Scotland Yard 1901, first conviction 1903), `bloodstain_grouping` (Lattes 1915) — while `cctv` 1960 and `automated_fingerprint_search` 1990 are **confirmed correct**, and the whole thing should merge with `period-kb`. **§9.6 is the gap no research can close:** for *Oscar Brodski* nothing defeats the trick by date — the story shows it beaten in 1912 by a competent detective — so `forbids` conflates *the era makes this impossible* with *a good detective beats this*, and only the first is a date. Everything in §9.7 is mechanical except reconciling the three family vocabularies (§5), which now blocks two paths rather than one.
- `ANALYSIS_77/ANALYSIS_77.md` — **the real-novel corpus has never been connected to anything (2026-08-31).** Full audit of the reference library of real crime novels — acquisition, encoding, storage, and every consumer that reads it. The library is **14 catalogued titles, ~77 KB, last touched 2026-02-13**: **1 is a zip the loader cannot open** (the Hound — and it is the richest file we own), **5 are in a `case_metadata` schema no loader, agent or validator in this repo reads** (and those 5 carry exactly the per-suspect alibis and named culprits `agent3-corpus-shadow-findings` reports the pipeline does not have), **2 are not real novels**, **4 contain material plot errors** including an invented character name, and **0 of 14 pass `validateCml`** — 8 of 9 readable files name no culprit at all. **Only 2 of 14 were published inside the Golden Age**, median year **1902**, while `period-kb` targets **1931**. **THE DECISIVE FINDING: the novelty audit cannot fail.** `agent8-novelty.ts:272` renders the FAIL band as `> Math.min(100, 90+10)%` = **"> 100%"**, so the regeneration gated at `agent3-run.ts:543` is unreachable — MEASURED **32 archived audits: 25 pass, 7 warning, 0 fail**, max similarity ever 0.88 — and the nearest neighbour is only ever one of **four** books, of which the **synthetic `the_second_key` placeholder ranks third (7×)**, ahead of every Doyle novel. At Agent 3 the corpus arrives with **three of its five prompt fields hard-coded dead** (`mechanismType` reads a `.type` no seed has; `constraintSpaceSize` reads a `.constraints` array that does not exist; `inference_path.length` on an object) — quoted verbatim from the 2026-08-31 live prompt — plus **raw filenames in `divergeFrom`** and **2 of 5 axes given nothing at all**. Corrects A_72 §10: the audit **does** fire on canary runs (`canary-core-inputs.yaml` passes 0.9, overriding the env's 1.0) and is skipped on the **API/product** path — a harness/product divergence in its own right. §8 answers *can we obtain more*: **yes** — US copyright has now expired through **1930**, opening Roger Ackroyd, Sayers, Crofts, Berkeley and Chesterton, and §8.3 notes the corpus stores **no prose at all**, so a fingerprint-only tier could cover the whole canon. **§9 is 14 fixes in cost order; items 1–7 are free and need no run.** **§7–§14 IS THE PLAN (added on request, public-domain-only).** Its spine: store the canon at the altitude where invention happens — the **shape of the deception**, which is genre grammar and cannot be plagiarised — then **generate into a structural cell no book occupies**, novel by construction and genre-fluent by construction at once; `cell-scheduler.ts` already exists at `shadow` and has only ever had our own runs to choose against. **Three corpora, not one**: breadth for divergence (60+ works), depth for exemplars (25), and a **calibration corpus of real period prose** — the first *upward-pointing* prose instrument this project would own, against A_75's finding that all 40 prose notes describe an absence. Safety is a design property, not a policing one: the generator is never shown a passage it could paste, snippets are capped at 25 words, and a **6-gram anti-copy gate baselined on our own 189 manuscripts ships before any snippet does**. A seven-stage ingestion pipeline (ACQUIRE→CLEAR→ENCODE→VERIFY→DERIVE→EXTRACT→GUARD) whose **clearance gate makes "public domain only" a property of the build** (UK life+70 ⇒ d.≤1955; US ⇒ pub.≤1930; unknown = red = build fails) and whose encoder **reads the book instead of recalling it** — the fix for the four hallucinated plots. §8.3 lists 21 GREEN Tier-A acquisitions each closing a named gap (Chesterton *Invisible Man* and Glaspell for the empty `behavioral`; Freeman *Red Thumb Mark*, Post, Mason, Van Dine for the empty `authority`; Whitechurch as the PD substitute for Crofts) plus the inverted, courtroom, armchair, impossible-crime and non-visual forms we hold none of. **§9.4: `authority` goes 0 → 2 by retagging two books we already own** — Big Bow and the Yellow Room are both detective-is-the-culprit and both mis-tagged. §8.2 flags the three works already on disk that are UK-AMBER (both Christies to 2047; **Bentley clears 1 Jan 2027**), §8.4 records the in-copyright fingerprint tier as **WITHDRAWN** on the owner's rule. **§12 is six phases, ~£65 and ~7–8 days, and Phase 0 is free** — repairing §5's severed paths before buying anything, because sixty novels poured into a prompt block that prints `Mechanism type: unknown` buy nothing. §13 states the falsifications first. **§15 is the VERIFIED reading list (added on request): 39 GREEN works, every one resolved against the live Project Gutenberg catalogue and cited by ebook id** — nothing listed on recall. Checking demoted four of §8.3's Tier A picks as **NOT OBTAINABLE** (Post's *Uncle Abner*, A.E.W. Mason's Hanaud novels, *The Tea Leaf*, Whitechurch's *Diana's Pool*), each recorded with a substitute so it is not re-proposed. The big win is **five Philo Vance novels (Van Dine, 1926–1930, PG 70634/70788/72719/75157/78655)** — genuine Golden Age proper, GREEN in both jurisdictions. `behavioral` 0→5+ (Chesterton ×3, Glaspell's *A Jury of Her Peers* inside PG 20872, Bramah's *Max Carrados*); `authority` 0→4+ (Freeman's *Red Thumb Mark* — a forged fingerprint — Van Dine's *Greene*, Post, Randolph Mason) on top of the two we already own once retagged. **§15.2 is the trap: Project Gutenberg is a US catalogue and hosts *Roger Ackroyd*, *Murder at the Vicarage* and 16 more Christies that are UK-copyright until 2047 — availability is not clearance**, and Bentley + Packard self-promote on 1 Jan 2027 when the gate re-runs. **§15.4 confirms 11 of our existing 14 are re-fetchable today**, so Phase 2 can run in full without waiting on any §14 decision. **§10.8 answers "how much of each book do we actually read": ALL of it, and two of the four layers read it for free.** MEASURED across seven §15 titles, a Golden Age novel is **412–592 kB ≈ 85k–125k tokens — about a TENTH of gpt-4.1's context**, so nothing needs chunking. That killed two design choices from the first draft: **(1) no map-reduce** — a clue in ch2 is only a clue given ch18, so chunking destroys the very relationship being extracted; **(2) VERIFY is `String.includes`, not a second LLM pass** — a model verifier can hallucinate agreement, `includes()` cannot, and "Ralph Hewitt" appears nowhere in Wallace so no quote containing it can pass, at zero marginal cost. Craft measures (100% of text) and span verification (100% of text) are both **deterministic and free**; the full CML encode reads the whole book **three times** — structure map on mini, case encode, then a dedicated exhaustive clue+inference sweep — not for context but for attention. Recomputed against `run-cost-audit.mjs`'s own price table, **the programme is £24 (gpt-4.1) to £58 (frontier), not £65** — the old figure was ~2.3× high per book because it priced the map-reduce and the LLM verifier that are now gone. Phases 0, 1 and 5 stay free. **§16 IS A PILOT RUN OF THE WHOLE PIPELINE ON ONE BOOK** — Chesterton's *The Invisible Man* (PG 204), taken through all seven stages, output in `library/works/the_invisible_man/`. **It PASSES `validateCml` — the first file in this project's history to do so** (§4.2: 0 of 14 existing seeds pass), and the novelty judge scores it `distinct` against all 19 reference fingerprints. **The result that matters is the negative control:** VERIFY ran 20 verbatim spans as literal substring matches **against the live source page rather than any text the run had authored** — 20/20 passed, and `"Ralph Hewitt"`, the invented character from our own *Twisted Candle* encoding, **correctly failed**. The six-month-old defect is caught deterministically at zero LLM cost. The gate failed once, on nine errors that were **all field names** — every substantive check (grounding, non-empty culprits, culprit-is-not-detective, concrete `required_evidence`, no duplicate observations) passed first time, so the expensive half worked and the cheap half is fixed by pasting the schema into the encoder prompt. Cost **£0.036** for the story; scaled to a novel it is £0.21/pass, **exactly what §10.8 predicted**. Two findings argument would not have produced: **(1) the 14-value `mechanism_family` enum cannot express this story** — DERIVE was forced to emit `impersonation` when Welkin impersonates nobody, he occupies a role whose occupant is not perceived as a person; `role_invisibility` proposed, and a missing family is a region of genre space the fingerprint schema, device library and cell scheduler all share and none can address. **(2) VERIFY must fetch its own copy of the source** — checking spans against a locally-written copy is circular and proves nothing, which promotes §10.1's SHA-256 from hygiene to load-bearing. Renaming note from the run: the library lives in **`library/`, not `corpus/`** — `.gitignore:52` already ignores `corpus/` (it is `CORPUS_SNAPSHOT_DIR`, per-run telemetry), so §10.7's original layout would have left every acquired work **silently untracked**. Found only by writing the first file to disk. **§16.9 = a second run on a £2 budget: two more works acquired, encoded and verified** — Glaspell's *A Jury of Her Peers* (PG 20872, domestic/non-elite register) and Orczy's *The Fenchurch Street Mystery* (PG 10556, armchair detection), both GREEN, both in `library/works/`. **Both PASS `validateCml` on the FIRST attempt** (vs §16.3's one failure), confirming §16.8's fix — the schema in the encoder prompt removes that whole failure class — and **20/20 spans each**, with negative controls (`"Sergeant Cuff"`, `"Inspector Lestrade"`) correctly failing. The span check again caught what reading would not: **two genuine mis-quotations** where I transcribed double quotes over source text that nests SINGLE quotes in reported speech. **THE ENUM FAILED AGAIN — 2 of 3 encodings cannot be expressed by the 14-value `mechanism_family` vocabulary.** *A Jury of Her Peers* was forced to `hidden_accomplice` when the story has no accomplice and no trick at all — the concealment is done by the investigators' own framing of what counts as evidence — **and the mis-tag then surfaced as a measurement**: the novelty judge's nearest neighbour became `butler_did_it`, a cliche it resembles in nothing but the two forced fields. So a wrong family does not sit quietly; it propagates into the divergence verdict and makes a distinctive case look tired. Only *Fenchurch Street* (`substituted_body`) fitted, which makes it the control. Second proposed addition: `investigative_blind_spot` alongside `role_invisibility`; the enum is now a **Phase-1 blocker**, not a nice-to-have. `behavioral` goes **0 seeds -> 2**. Freeman's *The Case of Oscar Brodski* (PG 59478, the story that invented the INVERTED form) was acquired and cached at **16,690 words** but deliberately NOT encoded — it exceeds the budget, and at 2x either work above it is the right subject to finally test §16.7's open question about one pass vs three. Budget lesson: **turns, not text, are the cost driver** — which is itself the argument for Phase 1 building the harness instead of doing this conversationally. **§16.10 completes the fourth work: Freeman's *The Case of Oscar Brodski* (PG 59478, 16,690 words) — the story that INVENTED the inverted form**, which the library held none of. PASS on attempt 1, **30/30 spans**, negative control (`"Sherlock Holmes"`) rejected, `distinct`, and `mechanism_family: staged_scene` **fitted cleanly with no forcing** — so the enum tally is **2 of 4**, not 1 of 3. **§16.7's open question is answered, with a caveat that matters: the text COULD NOT BE READ IN ONE CALL** — the first read returned lines 1-471 of 704 and truncated, and the entire reveal (the blood-direction argument, the surplus glass, the splinter fitted into the lens) was in the part that did not arrive; encoding from page one alone would have produced a confident, well-formed and WRONG case file. But that is a **harness limit (one call's output cap), not a demonstration that the model needs three semantic passes** — §10.8's three-pass design is argued from attention and this run does not test it, because the tooling constraint bit first and lower down. What it does establish: **at novella length the encoder cannot be a single call**, so Phase 1 must handle multi-call acquisition and hold the whole text across them; one-pass-vs-three now needs a full novel (*The Red Thumb Mark*, 416 kB) to settle. Third run, third genuine mis-quotation caught by `String.includes` ("it consisted of" vs the text's "to consist of") — **running tally: 3 encoder errors caught by the span check, 0 by reading.** **LIBRARY STATE: 4 works, 4/4 validator-clean against 0/14 for the legacy `examples/`, 90/90 spans, 4/4 negative controls rejected**, `behavioral` 0 -> 2, and four forms added that the library held none of (role-invisibility, domestic register, armchair, inverted). Next: *The Red Thumb Mark* — the `authority` case AND the length test in one.
- `ANALYSIS_76/ANALYSIS_76.md` — **the five things between us and 90, and what to actually build (2026-08-29).** Implementation plan. Four of the five issues are downstream of one: we cannot measure whether a change worked, so the measurement fix is ordered first. Contains the sharpened arithmetic (best sum ever 83; 90 needs 84-85 with a good offset), the case for retargeting at `atmosphere` rather than `prose`, and the operations-not-statistics design move that follows from the VoiceSpec falsification. Total cost of the plan: ~£1 plus the owner's reading time.
- `ANALYSIS_75/ANALYSIS_75.md` — **prose has never been praised, and the ladder it is on stops at 8 (2026-08-26).** One-category board, written on request. `prose` is the lowest-scoring of the ten (mean 5.6 over 41 reads), furthest from its ceiling on the best books (top-8 mean 6.9 vs max 8), and has never been given a 9. **THE FINDING: every prose note ever written describes an ABSENCE — 38 of 40 name leakage or artifacts, 20 of 40 are comparative (*"cleaner"*, *"fewer"*), and 2 of 40 contain a positive adjective, both of them "strong … but". The reader has never praised a sentence.** The 4→8 progression is one axis — how many artifacts they tripped over — so 8 is where the scale runs out. **AND THERE IS NOTHING ELSE VARYING TO REWARD:** across the 15 recent manuscripts, mean sentence length, within-book variance, short-sentence share, dialogue density, semicolons and em-dashes show NO signal against the mark, and between-book variation in sentence length is **10% of within-book variation** — fifteen books drawn from one distribution of sentences, the same uniformity X94 found in openings and A_74 §11.4 found in the cast, now at sentence level. Corrects two assumptions on the way: injector leakage is **necessary but not sufficient** (both prose-8 books have zero, but so do three prose-6 books), and repeated sentences are **not** a driver (the prose-8 books contain the MOST). Also finds that leakage is a REGISTER not a string list — one book scored 6 for *"major validation leakage"* that no template probe can see. Pronoun work is closed (zero mentions at prose ≥6). Tier 1 is that nothing asks for a sentence worth noticing, nothing gives a story its own voice, and every prose signal we own is negative. The proposal with the right shape is a per-story prose register chosen and committed like a device — and the doc states plainly that this is a hypothesis with one precedent, whose cheapest test is a rewrite-and-read, not a build. **§6 is the resolution plan** and it holds each item to three properties — a RULE rather than a repair, an instrument that points UPWARD (every prose signal we own counts defects, so the category has no gradient above zero), and a STANDING GUARD, since §3's uniformity took 41 reads to notice and nobody would notice it returning. P1 is a per-story `VoiceSpec` generated/judged/committed like a device, with a deterministic conformance metric and a falsification stated before building; P2 replaces the template registry with a four-feature REGISTER score (measured: 6/6 machine sentences at threshold 3, 0.5% false-positive at 4, and it catches the leakage no template probe finds); P3 is the rule that text the pipeline writes for itself is held to the same standard as text the model writes; P4 clears the four built-but-unrun flags first so the next read is interpretable. About £2 and two reads before the decisive verdict. **§7 is the build record (2026-08-27): everything free is built, tested and committed, and two items changed shape when they met a measurement.** P2's regeneration trigger DIED ON ITS OWN PROBE — rebuilt as a discriminative test against the 40 human-marked manuscripts, threshold 3 holds (rho = −0.421, monotone 15.9% at prose 4 to 10.0% at prose 8) and threshold 4 does not (rho = +0.207, wrong sign), so the telemetry ships and the lever does not; what it flagged in the prose-8 books were good sentences. The voice guard found on its first run that the between/within ratio is 27.1% over all 188 manuscripts and 12.6% over the last 15 — the variation the corpus has was put there by OUR edits over a year, not by any story choosing a voice. P1 ships as a full engine (generate → judge → commit, a measured `VOICE_CORPUS` of 20 books spanning just 3.6 words against a within-book sd of 7.8, and a deterministic conformance metric) behind `AGENT9_VOICE_SPEC`, with its falsification stated before the build.
- `ANALYSIS_72/ANALYSIS_72.md` — **the reader gives nines to distinctiveness, and this pipeline has one engine for it (2026-08-23).** Written after the first external read of a four-flag run returned **81/100**, and against evidence that did not exist the day before: **36 manuscripts with a cold read beside them on disk, 35 with a full ten-category table — 350 human marks with a reason attached to each**, where every previous ordering was drawn from eight transcribed by hand. Re-derived at n=35, best-ever-everywhere sums to **85** with offset mean **3.9**, so a manuscript at its best in all ten categories still projects to **88.9**. **THE FINDING:** splitting the reader's own vocabulary by the mark attached to it, a **9** goes to what is DISTINCTIVE (*distinctive, memorable, fits, physical, emotionally*) and an **8** goes to what is CORRECT (*good ×23, clue, body, clear, stable, setup*) — and all seventeen `opening_hook` 8s are literally the same sentence: an inventory of props followed by a compliment. **`premise` holds 7 of the 14 nines ever awarded, and `premise` is the one category with a novelty engine** (Agent 3b's novel devices + Agent 8's corpus audit); `atmosphere`'s three 9s all praise setting-MECHANISM fit, the same engine at one remove. `character_clarity`'s whole 2→8 ladder measures only whether the reader can tell who is who — **no note in 35 reads calls a character interesting** — because every mechanism aimed at it is an error suppressor. Also: X93's fold removed the *"Chapters 9–10 repeat the clearances"* complaint for the first time in the corpus **and pacing stayed at 8**; X94 produced the first non-`character-action` opening ever **and the hook stayed at 8**; the `prose` 6 and the `dialogue` complaint trace to ONE cause — the locked-fact verbatim contract, which put *"a ten minutes past eleven"* in the book ten times and the same seven words in **15 of 89 dialogue spans**; the ordinal judge resolves ~10 marks and not 5 (67% on close pairs, p=0.25); and the external reader's own error bar is measured for the first time at **±1** (nine of ten categories identical across two reads of near-identical text). **Tier 1 is a missing capability, not a defect: a distinctiveness engine for openings and for people.** **§10 is the plan.** Its load-bearing correction is that a FLOOR of 90 must be priced at the offset a floor has to survive — **+2, which five of the ten reads at 80+ returned** — so 90 needs a category sum of **88: eight 9s and two 8s on every run**, against a corpus whose best single manuscript sums 83. That decomposes into three problems with three different medicines (A: three categories from never-9 to 9, via the engine; B: five intermittent 9s made reliable; C: dialogue and prose held at 8 by paying down the locked-fact tax), plus D: a floor is a claim about the worst of N and every quality claim here has been n=1. The transplantable ingredient is narrower than it first looked — Agent 8's audit **cannot fire** (`NOVELTY_SIMILARITY_THRESHOLD=1.0`), so what earns premise its 9s is Agent 3b's generative half alone: **five divergent candidates in one call against an explicit divergeFrom list, then commit the winner as a downstream contract** — one design-tier call, cheap enough to run three times. Nine free items are sequenced before any further spend; C4 (a canonical form plus registered aliases, so fair play survives while fifteen identical dialogue lines do not) is the only item that lifts two capped categories at once; and §10.7 states what would falsify the plan, including the result that would reopen the generation-model question Tier 4 currently closes.
- `ANALYSIS_71/ANALYSIS_71.md` — **the measurement layer, fixed at source (2026-07-31).** Answers A_70 §4's "most valuable unanswered question" and closes three long-standing defects that were all the same shape: **a detector reading a path the data never travelled**, failing silently and confidently for weeks. **(1) Report finalization** — `save()` ASSERTS invariants, so a violation means the report is never written; `failed_phase_signal_cannot_have_passed_outcome` predates A_65b Ph1.3 (which redefined `run_outcome` as the SHIP signal and moved the phase verdict to `phase_thresholds_met`), so "shipped but a phase missed threshold" — the designed state, and Cast Design misses on every run — threw. The 2 reports that finalized are exactly the 2 with `phases_failed: 0`. Also corrects A_70's "no run ever finalized": that check read `phases[].name`, the field is `phase_name`. Partials are now honest at WRITE time (`run_outcome: "in_progress"`), not at cleanup time, so a crash can't strand a "96 / A / passed" snapshot. **(2) Cast Design was never the weakest phase** — Agent 2 emits `relationships` as a bare ARRAY (15/9/10 pairs measured across three runs); every reader guarded on `Array.isArray(relationships.pairs)`, so each character got an EMPTY list, two quality tests scored 0 in every report ever written, and `normalizeRelationshipTension` never ran. Fixed at source; quality **60 → 100** on real run data with no change to Agent 2. **(3) The clearance prose has a source** — A_70 §2's 0/14 measurement HOLDS (now 0/18) but cleared the wrong body: the prose both 07-31 reviewers quoted comes from `buildDeterministicClearanceParagraph` in `deterministic-repair.ts`, a second injector in a second package, whose 3 call sites `AGENT9_REGEN_SUSPECT_ELIM` only partly gates. Plus: red-herring FLOOR (the budget was a ceiling — zero satisfied it), Azure content-filter refusals counted by family, `entity_pronoun_drift_count` baselined **with its A_66 detector-change boundary marked** (0,0,0,0 pre vs 6,27 post — NOT comparable), and the rubric judge model recorded on the artifact. **Rule added: verify a detector's read path against a real artifact, not against the declared contract.**
- `ANALYSIS_70/ANALYSIS_70.md` — **the outstanding-issues board after the 2026-07-27 pool run.** Every claim marked MEASURED / INFERRED / UNBASELINED. Falsifies A_68 §8.1's "(common)" frequency claim (injector template 0/14 shipped stories); finds `"In a remembered moment, "` ×28 in one shipped story as a deterministic injection the polish prompt is instructed to delete *before it exists*; escalates stale-report integrity; first observation of Azure content-filter refusals blocking the pipeline's own regens; and the `AGENT9_FULLSTORY_POLISH` A/B result — **a silent no-op** (normalized n-gram needles matched against raw text, so the phrase path had never worked). *(§2 and §4 amended by A_71.)*
- `ANALYSIS_69/ANALYSIS_69.md` — **what a frontier model on the whole story is worth, and how to spend it.** Separates model-tier from context-breadth; rejects the one-shot whole-story rewrite (it bypasses every per-chapter gate); builds Increments 2 (`AGENT9_MODEL_POLISH`) and 3 (`AGENT9_FULLSTORY_DIAGNOSTIC`, mode-valued, `true`→shadow-not-apply). Smoke probe corrected the price ~10× (a whole-story polish is ~15p, not £1.40), exposed a placement bug (the diagnostic ran in `generate.ts`, but prose keeps changing after it returns — anchor rate 60% → 100% at the ship layer), and surfaced raw clue IDs (`rh_1`) dramatized as story content.
- `ANALYSIS_62/ANALYSIS_62.md` — **the measurement was wrong, and the missing lever isn't missing (2026-07-16).** Two findings from the M1v3 batch + a from-source recomputation of all 20 rubric-scored shipped runs. **(1) The cap ledger was hand-tallied and materially wrong** — M1v2-1 logged "3 caps" (actually **5**), M1v2-5 "2 caps" (actually **7**); `templateLeakageHits` was recorded `1/11` but is **7/20 (7/15 M1-era — the most frequent single cap on current code)**; "13 shipped runs" was a stale `/13` cap denominator, not a count. **The −25% P4 rescope was justified by those numbers** → its premise is falsified (RC-3), as is "identity is the cap-free theme" (M1v2-5/identity fired **7 caps**, the most of any run). **(2) Item 17's "missing" regen lever is already built** — `detectTemplateLeakage` + `noTemplateLeakageValidator` (`prose-guard/fidelity.ts:155/165`), the `"leakage"` defect kind (`regen-repair.ts:35`) and its regen instruction (`regen-integration.ts:109`) **all ship today; nothing emits the defect.** ~10–30 lines of wiring, not a new lever + count restart. **RC-2 states the general rule: a cap has a lever iff its detector is reachable from the generation loop** — `prompts-llm` never imports `@cml/rubric-score` (three files hand-mirror it in comments); `@cml/prose-guard` is the sanctioned shared home, and scaffold already proves the pattern. Key numbers: caps on **14/15** M1 runs, **`prose` capped ≤4 on 10/15, prose mark mean 4.80/8** (the largest gap on the board, cap-driven not craft-driven), median rubric **64** vs the M2 bar 73, flat across three batches (63→64→62) because every fix so far was an abort-class repair (reliability, not ceiling). **P4 as scoped cannot deliver M2** — template-leakage fires with **no** scaffold cap on 4/15. 4-phase plan: P1 derive-the-panels script (no runs); P2 wire the leakage lever **default-OFF** (count-safe); P3 A/B leakage + scaffold together; P4 Item 9 (`detectDualValueNoContrast` promotion) + the detector sweep → M2. Includes the do-NOT-do mole list.
- `ANALYSIS_61/ANALYSIS_61.md` — **consolidated outstanding-actions register (whack-a-mole-aware), 2026-07-02.** Everything still to do from A_58/A_59/A_60 + the two 2026-07-02 validation runs, organised **by root cause, not by instance**, each tagged **Structural** (dissolves a class) or **Tactical** (unblock-only). §0 records **A_60 Phase 1 landed** (season FP, novelty ENOENT junction, triage). Root causes: **RC-1** injectors write prose → inject→regen (P3/P4); **RC-2** facts re-derived not Bible-dereferenced → `AGENT9_BIBLE_AUTHORITATIVE` (**contains the current release-blocker RC2.1 `locked_fact_missing_value` "thirty feet"**); **RC-3** upstream case gaps → Agents 3/3b/5/7 (**run-killer RC3.1 Agent-5 coverage hard gate**; RC3.2 sundial plausibility P0.5; RC3.3 transition-bridge); **RC-4** verifier-precision FPs (the mole-prone, capped class); **RC-5** craft (critique→rewrite P5, tiering P6). Includes the "do-NOT-do" mole list, a priority ranking (RC2.1 + RC3.1 first), and a **4-phase implementation plan** (§5, test-runnable after each; one structural lever per phase, N≥4-gated): P1 done; **P2 = complete-the-run** (Agent-5 repair-not-abort + flip `AGENT9_BIBLE_AUTHORITATIVE` for locked-fact/pronoun/season-principled); P3 = de-template (inject→regen + P0.5 + outline beats); P4 = craft (critique→rewrite + tiering) + delete injector layer. Supersedes the scattered A_58/59/60 fix-lists for tracking.
- `ANALYSIS_60/ANALYSIS_60.md` — **run-review (*The Sundial's Shadow*, 2026-07-01 11:45; internal 74/C, `aborted`, 0 critical / 11 major).** Catalogued by agent → type → known-issue → where prior fixes fell short. **Phase-0 beat-abort fix validated live** (Agent 7 clean, full pipeline completed). The abort is **one dominant false positive**: `august vs spring` fires on the *spring-loaded blade* weapon — mechanical "spring" the temporal stripper (`SPRING_MECHANICAL_RE`) doesn't cover ("spring-loaded" hyphen; "a spring or a blade"), **unrepairable** (season-lock protects mechanical springs), prose even says "midsummer sun" → releasable draft blocked by an FP. Recurring known debt: **novelty ENOENT (A_58 #5) — src fix written but dist never rebuilt** (worker-consumes-dist trap, still skips); implausible sundial thermal-expansion physics (A_59 #5 / P0.5, not landed); ch6 verbatim clue transcription (A_59 #1/#4, LLD P3 default-off); clearance/resolution injectors + 21 pronoun repairs (LLD P1/P3–P5). None caused by the Phase-0 or regen-clue-staging changes. **Includes a root-cause reframe** (pre-resolution already works — the Season Lock produced "midsummer sun"; the leak is *validators re-deriving* facts instead of dereferencing the frozen Bible) and a **3-phase fix plan, test-runnable after each phase** (P1 unblock: season regex/adjacency + novelty dist rebuild; P2 freeze-and-dereference: season→Bible fact + flip `AGENT9_BIBLE_AUTHORITATIVE`; P3 regen+plausibility: P0.5 + `AGENT9_REGEN_CLUE`/P4).
- `ANALYSIS_59/ANALYSIS_59.md` — **run-review (*The Sundial's Silent Verdict*, 2026-06-30 13:04; ChatGPT 74/100, internal 74, release gate failed).** Strongest draft yet, blocked from the 80s by prose-layer defects. P0: deterministic clue/inference-surfacing templates shipping RAW key-terms into prose ("pressed on to the next concrete detail … the trail bent toward …", deterministic-repair.ts:294/300/376 — the A_57 D4 class never covered); cross-character pronoun errors (D5; the `2e32d698` partial-revert fix applies); report-style clearances + detective self-clearance; DT-scene "two competing readings" validation leak. P1: implausible sundial physics (brass expansion → 10-min shift; Agent 3b plausibility); poison under-planted in Ch1. P2: repeated "truth remained elusive"/"evidence was mounting". Includes a phased fix plan.
- `ANALYSIS_58/ANALYSIS_58.md` — **run-review (Edith Langley / Reggie Harcourt, 2026-06-30).** Narrow catalogue of the concrete issues in ONE run log (completed but `needs_revision`, 9 major; 5/10 batches retried). P0: ch10 reveal non-convergent (resolution injected, not generated), dead victim animated ×10, G4 DT-scene outline clue-scheduling gap. P1: `VICTIM ALIVE` false-positive on non-victim Agnes, novelty schema ENOENT (shadow judge skipped), F5b "rain-speckled" verb false-positive. P2: retry cost (mechanism/location-inventory/quotes/clue), `signatureTic` schema drift, locked-fact injections, pronoun drift, whitespace padding.
- `ANALYSIS_57/ANALYSIS_57.md` — **strategy: prose into the 80+ ChatGPT band.** Deep-dive on run `09168377` (internal K2 80 / ChatGPT 70). §1–8: seven generic, parameter-agnostic defect classes (D1 verbatim-locked-fact garble, D2 self-contradicting central clue, D3 mechanism-environment incoherence, D4 injection template prose, D5 pronoun instability, D6 functional dialogue, D7 abstract test) + the meta-lever (teach the internal judge to see the polish defects so we stop optimising blind). **§9: the architectural deep-dive** — collapses the seven symptoms into three root solutions, one per quality pillar (Coherent ← a canonical world-state ledger; Logically sound ← a discriminator verifier; Interesting ← dramatization-not-declaration) + the generation contract (construct→verify→draw→dramatize→judge-all-three) and a best-value build order. Prioritised P0/P1/P2 with code/measurement/model-tier disposition.
- `ANALYSIS_56/ANALYSIS_56.md` — **CURRENT source of truth for agents 1–8.** Consolidated "what's left" register, verified against live code by a 19-agent workflow (240 candidates → mostly already-done; docs lag code). Defines the implementation line (Waves A–C: deterministic code) vs the separate flag-flip-and-measure and research/defer tracks. Records that A_55 #2/#3b/#4/#5 already closed several items the older docs still list open.
- `ANALYSIS_55/ANALYSIS_55.md` — failure-run review (K2=0 false-zero) + the 5 root causes (all fixed): parser object-map shape, mechanism-gate enforcement, culprit-near-death FP, victim active-after-death, completion-first severity, G4 gap.
- `ANALYSIS_54/ANALYSIS_54.md` — 3-run validation of A_53; cross-run-novelty cost inconclusive/confounded (kept shadow); top findings (culprit-near-death FP, scene-grounding FN, G4 gap, resolution fragility).
- `ANALYSIS_53/ANALYSIS_53.md` — the 11-phase repair-not-abort tracker (~98/103 done) + deferrals.
- `ANALYSIS_52/ANALYSIS_52.md` — **the role model + the remaining work** — supersedes A_51. K1 (named victim) + K2 (rubric honesty) landed and the pipeline now completes (K2 honest in situ, 62/100); formalizes the cast **role model** (1 detective + 1 victim + n-2 suspects, n≥5) that closes the victim dead-and-alive class, and re-orders the remaining work around triad-effectiveness-in-prose. *(Directory doc — distinct from the older flat `ANALYSIS_52_run_970b323c_error_warning_report.md`.)*
- `ANALYSIS_51/ANALYSIS_51.md` — outstanding issues, restated (post-triad register) + the detailed K1/K2 build, two live diagnoses, and Fix #1/Fix #2 record (§3.1–§3.7). **Superseded by A_52 for tracking; retained as the build history.**
- `ANALYSIS_50/ANALYSIS_50.md` — outstanding issues by agent (consolidated register + the §9 structural-pivot investigation + triad fixes). **Superseded by A_51 for tracking; retained as history.**
- `ANALYSIS_49/ANALYSIS_49.md` — implementation plans for the four deferred A_48 items (novelty store, clue restructure, Agent 7 scheduler+budget, voice gate)
- `ANALYSIS_48/ANALYSIS_48.md` — agents 1–8 to 90% prose-fitness (vanity-scorer + shadow-machinery thesis) so Agent 9 can reach 80
- `ANALYSIS_47/ANALYSIS_47.md` — upstream-vs-prose ablation + ROADMAP_TO_80/90
- `ANALYSIS_44/ANALYSIS_44.md`
- `ANALYSIS_43/ANALYSIS_43.md`
- `ANALYSIS_42/ANALYSIS_42.md`
- `ANALYSIS_41/ANALYSIS_41.md`
- `ANALYSIS_40/ANALYSIS_40.md`
- `ANALYSIS_28/ANALYSIS_28.md`
- `ANALYSIS_27/ANALYSIS_27.md`
- `ANALYSIS_26/ANALYSIS_26.md`
- `ANALYSIS_57_agent4_required_evidence_checklist_status.md`
- `ANALYSIS_56_run_20f9ca27_prompt_review.md`
- `ANALYSIS_55_run_34d713c0_error_warning_report.md`
- `ISSUES_05.md`
- `ANALYSIS_54_run_71e31ebd_error_warning_report.md`
- `ISSUES_04.md`

## Run Triage Table

| Date       | Analysis | Run ID (short) | Status | Primary failure class |
|------------|----------|----------------|--------|-----------------------|
| 2026-07-16 | `ANALYSIS_62` | 20 runs re-derived (M1v3 `…6189809`…`…9997051`) | register (measurement + ceiling) | **Measurement failure, not a run failure.** Hand-tallied cap panel wrong (M1v2-1 3→**5**, M1v2-5 2→**7**); `templateLeakageHits` `1/11`→**7/20** (7/15 M1-era, the most frequent cap on current code) — **the −25% P4 rescope rests on the bad figure**; "identity = cap-free theme" dead (M1v2-5 fired 7 caps). Ceiling class: **`prose` cap-pinned ≤4 on 10/15, mean 4.80/8**; median 64 vs bar 73, flat over 3 batches. **Item 17's lever is built but unwired** (detector + validator + `"leakage"` kind + instruction all ship; no emitter) → wiring fix, count-safe at default-OFF. RC-2 rule: *a cap has a lever iff its detector is reachable from the generation loop.* Also: M1v3-5 lost to host power (standby → critical battery), **not** an abort — must never restart the count. |
| 2026-07-02 | `ANALYSIS_61` | `bfmz7izf6` / `bf6khyl2r` | register (Phase-1 validation runs) | **A_60 Phase 1 confirmed live** (novelty ENOENT gone; season no-regression on a clock/mainspring mystery). Run 1 died at **Agent-5 coverage hard gate** (inference step with no covering clue — run-killer, RC3.1); run 2 reached final validation → `needs_revision` 7 major = **6× `locked_fact_missing_value` "thirty feet"** (RC2.1, the Phase-2 target) + 1× `missing_case_transition_bridge` (RC3.3). Consolidated outstanding register by root cause. |
| 2026-07-01 | `ANALYSIS_60` | `0d45c651` | aborted (74/C, 0 crit / 11 major) | **Beat-abort fix validated (pipeline completed).** Abort driven by **one false positive**: `august vs spring` on the *spring-loaded blade* weapon — `SPRING_MECHANICAL_RE` misses "spring-loaded" (hyphen) + "a spring or a blade"; unrepairable (season-lock protects mechanical springs); prose says "midsummer sun". Recurring known debt: **novelty ENOENT (A_58 #5, dist never rebuilt)**, sundial thermal-expansion physics (A_59 #5/P0.5), ch6 verbatim clue (A_59 #1/#4, P3 off), clearance/resolution injectors, 21 pronoun repairs. |
| 2026-06-24 | `ANALYSIS_50` | `9b824eb2` | complete (10/10) | **First clean corrected-build baseline.** Tier-1 keystones **HELD** — death-method / pronoun / season-lock killers did not recur (every structural judge-flag passes; no churn, no abort). Rubric **71 LLM / 68 shadow**. Loss now concentrated in *open levers*: `pacing 6` + `opening_hook 6` (Agent 7 mechanical repetition — same timeline contradiction restated ×4 in Ch1; chapters ~1.0k–1.46k words, so **T1.2/T2.11, not T1.3**) and `premise 7` (Agent 8 novelty **skipped**, threshold ≥1.0). New polish leaks: **title scaffold** ("Generated in scene batches…") + **`the the`** doubled-word. |
| 2026-06-19 | `ANALYSIS_44` | `70ac132d` | incomplete (died at Ch4/10) | Validation run for ANALYSIS_43 G/H/I. A–D content fixes confirmed holding (0 leakage/setting/reveal/pronoun in committed Ch1–3; D2 gate caught+cleared a Ch3 `mechanism access point` leak). G/H/I **never exercised** — they run at final validation, but the run died in per-chapter prose generation. New run-killer = template linter `template_bleed` (repeated paragraph openers) firing every chapter, no exhaustion backstop → no story produced; report frozen green 97.85/A `in_progress`. |
| 2026-06-18 | `ANALYSIS_43` | `1d55f7c7` → `5411ba7f` | aborted (fixes landed) | Baseline 1d55f7c7: verbatim schema/test leakage + post-LLM pronoun corruption & setting dump + dead disclosure embargo (premature reveal) — Layers A–D landed. Follow-up 5411ba7f (74/C, aborted): those killers did NOT recur; new blockers = victim-alive/identity slip (Ch3) + male→female pronoun miss (Ch5), both deterministically repairable but routed to terminal abort (recs G/H/I). |
| 2026-06-16 | `ANALYSIS_42` | `bdaf4d70` | warning | False-green passed run: upstream timeline contradiction + locked-fact injection + deterministic fallback prose regression |
| 2026-06-15 | `ANALYSIS_41` | `88ed23b2` | failed | Agent9 prose abort at chapter 9 after retry/fallback exhaustion; final reveal completeness gate not satisfied |
| 2026-05-28 | `ANALYSIS_28` | `17799935` | warning | Deep-dive errors: template_leakage Ch5/7; completeness_structure Ch7/10; clue_timing Ch10; 4 major post-gen validators |
| 2026-05-28 | `ANALYSIS_27` | `17799935` | warning | G6-Q1 culprit full-name gate false positive; 4 major issues; ChatGPT prose 72/100 |
| 2026-05-28 | `ANALYSIS_26` | `17799884` | failed | Cast Design abort (score 85 < threshold); locked fact value omission; pronoun false positives |
| 2026-05-07 | `ANALYSIS_07` | `17781744` | failed | Fair-play structural abort before prose with novelty telemetry/scoring mismatch |
| 2026-04-24 | `ANALYSIS_57` | `20f9ca27` | partial | Checklist audit: Agent 4 prompt contract implemented; acceptance gate still open (suggest replay still shows `cml.required_evidence_missing`) |
| 2026-04-18 | `ANALYSIS_56` | `20f9ca27` | failed | Agent 4 revision non-convergence after fair-play escalation; prompt-contract drift in retries and audit traceability |
| 2026-04-14 | `ANALYSIS_55` | `34d713c0` | failed | Agent 5 discriminating-test evidence mapping + weak suspect elimination/alibi gate failure |
| 2026-04-14 | `ANALYSIS_54` | `71e31ebd` | failed | Agent 5 suspect-coverage hard gate failure after retry |
| 2026-04-14 | `ANALYSIS_53` | `27958eec` | failed | Agent 9 pre-prose CML integrity contradiction abort |
| 2026-04-13 | `ANALYSIS_52` | `970b323c` | failed | Agent 9 chapter generation runtime exception |
| 2026-04-13 | `ANALYSIS_51` | n/a | strategy doc | Pre-prose enrichment strategy review |

## Analysis Reports
- `ANALYSIS_50/ANALYSIS_50.md` — outstanding issues by agent (consolidated register)
- `ANALYSIS_44/ANALYSIS_44.md`
- `ANALYSIS_43/ANALYSIS_43.md`
- `ANALYSIS_42/ANALYSIS_42.md`
- `ANALYSIS_41/ANALYSIS_41.md`
- `ANALYSIS_40/ANALYSIS_40.md`
- `ANALYSIS_07/ANALYSIS_07.md`
- `ANALYSIS_57_agent4_required_evidence_checklist_status.md`
- `ANALYSIS_56_run_20f9ca27_prompt_review.md`
- `ANALYSIS_55_run_34d713c0_error_warning_report.md`
- `ANALYSIS_54_run_71e31ebd_error_warning_report.md`
- `ANALYSIS_53_run_27958eec_error_warning_report.md`
- `ANALYSIS_52_run_970b323c_error_warning_report.md`
- `ANALYSIS_51_pre_prose_enrichment_strategy.md`

## Issue Lists
- `ISSUES_05.md`
- `ISSUES_04.md`
- `ISSUES_03.md`
- `ISSUES_02.md`
- `ISSUES_01.md`

## Archive
- See `archive/` for older `ANALYSIS_*` reports.
