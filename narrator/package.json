{
  "name": "cml-narrator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Azure AI Speech narrator - clone a voice from a sample, narrate a long story to an audiobook.",
  "scripts": {
    "start": "node src/server.js",
    "check": "node -e \"for (const f of require('fs').readdirSync('src')) require('child_process').execFileSync(process.execPath,['--check','src/'+f],{stdio:'inherit'}); console.log('syntax ok')\""
  },
  "dependencies": {
    "dotenv": "^17.2.4",
    "express": "^4.21.2",
    "ffmpeg-static": "^5.2.0",
    "multer": "^1.4.5-lts.1"
  }
}
